# React 19 is newer than some native deps' declared peer ranges (vision-camera,
# fast-tflite, etc.), so allow installs to proceed past those peer warnings.
# Applies to local installs and CI `npm ci` alike.
legacy-peer-deps=true
